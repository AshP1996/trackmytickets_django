# config/settings/__init__.py
#
# INTENTIONALLY EMPTY — do NOT add `from .base import *` here.
#
# The active settings module is controlled by the DJANGO_SETTINGS_MODULE
# environment variable.  Examples:
#
#   Local development : config.settings.dev
#   Production        : config.settings.prod_override
#   Plain production  : config.settings.production
#
# Set it in your shell, .env, docker-compose, or manage.py.
